// Default config
// Todo update and remove from repo
"use strict";
let config = {
  mysqlDB: {
    sqlHost: '127.0.0.1',
    sqlDB: 'scheduler',
    sqlUser: 'root',
    sqlPass: ''
  }
};

module.exports = config;